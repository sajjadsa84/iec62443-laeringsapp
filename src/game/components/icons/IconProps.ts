export interface IconProps {
  className?: string;
  size?: number;
}

/** Delt for alle ikoner: 32x32 viewBox, currentColor, samme strektykkelse/hjørneradius på tvers av settet. */
export const ICON_STROKE_WIDTH = 2;
